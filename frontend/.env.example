# Frontend Environment Variables (Example - DO NOT USE REAL VALUES)
# Replace these placeholders with your actual values

# Backend API URL (update to your deployed backend URL in production)
NEXT_PUBLIC_API_URL=http://localhost:8000

# Authentication Secret (MUST match backend BETTER_AUTH_SECRET)
# Use same value as backend's BETTER_AUTH_SECRET
NEXT_PUBLIC_AUTH_SECRET=your-secure-random-string-32-characters-long

# Application URL
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Optional: App Name
# NEXT_PUBLIC_APP_NAME=Todo App
